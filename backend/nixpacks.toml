[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "libxml2", "libxslt"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
